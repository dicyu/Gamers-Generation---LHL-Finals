const router = require("express").Router();

module.exports = () => {

  router.post("/", (req, res) => {

    // let query = "INSERT INTO messages (message, date) VALUES ($1, TIMESTAMP())"
    // db.query(query, )
  })

  return router
}